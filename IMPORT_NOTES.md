# Import notes

The upstream `master` history is retained as the ancestry of this repository's `main` branch.

Upstream GitHub Actions workflows were moved to `.github/upstream-workflows-disabled/` during import. They must be reviewed and adapted before being enabled in this repository.

No application code was modified during the import. Functional modernization and Geo Analyzer 2 integration should be implemented in separate commits and branches.
